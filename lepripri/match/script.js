/* ===================================================== PRIPRI MATCH — SCRIPT FINAL STABLE énergie par défaut : 100 ===================================================== */ /* =============================== CONFIG GLOBALE ================================ */ const GRID_COLS = 7; const GRID_ROWS = 8; const MAX_ENERGY = 100; const ENERGY_REGEN_TIME = 120000; // 1 énergie / 2 min const NON_FUSIONABLE_PREFIX = ["CFR", "CEN"]; const JOKER_ID = "JOKER"; const COMMAND_PERSONS = [ { id: "camille", img: "camille.png", levelBonus: 0.1 }, { id: "farceur", img: "pripri farceur.png", levelBonus: 0.1 }, { id: "gourmand", img: "pripri gourmand.png", levelBonus: 0.3 }, { id: "dixo", img: "dixo.png", levelBonus: 0.2 }, { id: "maman", img: "maman pripri.png", levelBonus: 0.1 }, { id: "papa", img: "papa pripri.png", levelBonus: 0.1 }, { id: "voyageur", img: "pripri du bout du monde.png", levelBonus: 0.1 }, { id: "plancequot", img: "plancequot.png", levelBonus: 0, noReward: true }, { id: "intelligent", img: "pripri inteligent.png", levelBonus: 0.15 }, { id: "djixy", img: "djixy.png", levelBonus: 0.1 } ]; const PERSONS = { plancequot: { id: "plancequot", name: "Plancequot", img: "icons/plancequot.png", rewardMultiplier: 0, levelGain: 0, isPoor: true } }; const DAILY_CHALLENGE = { date: null, tasks: [], completed: false }; /* =============================== fenêtres ================================ */ var closeEnergyAssistant = () => {}, nextOnEnergyAssistant = () => {}, previewOnEnergyAssistant = () => {}; function openEnergyAssistant() { var energyAssistant = document.body.appendChild(document.createElement('dialog')); energyAssistant.innerHTML = <div class="windowTitle">Assistant d'énergie</div><div class="content"><section><article><h1>vous n'avez plus d'énergie</h1><h3>obtenez plus d'énergies pour continuer à produire des objets</h3><h6>Dans cette assistant, vous aurais des OFFRES pour OBTENIR de l'ÉNERGIE GRATUITEMENT. Si vous n'avais pas de pièce de jeux le pripri, vous pouvez GAGNIER des PIÈCES et de l'ÉNERGIE en jouant à d'AUTRES JEUX le pripri.</h6></article></section></div><div class="buttons"><button onclick="nextOnEnergyAssistant()">suivant</button><button onclick="previewOnEnergyAssistant()" disabled="">précedant</button><button onclick="closeEnergyAssistant()">quitter l'assistant</button></div>; energyAssistant.onclose = () => { energyAssistant.remove(); }; closeEnergyAssistant = () => { energyAssistant.close(); energyAssistant.remove(); }; } function getCollectionFor(id) { const prefix = id.substring(0, 3); const collection = []; for (let i = 1; i <= 10; i++) { const testId = prefix + i; if (OBJECT_NAMES[testId]) { collection.push(icons/${testId}.png); } } return collection; } function moreInfo(objectNiv, collection, collectionIndex = 0) { const infoPanel = document.createElement('dialog'); infoPanel.innerHTML = <div class="windowTitle">Informations sur l'objet</div> <div class="content"> <h1></h1> <div class="collection"></div> </div> <div style="height: 85px;padding-inline: 19px;"> obtenu par : <div class="from"></div> </div> ; document.body.appendChild(infoPanel); infoPanel.querySelector("h1").textContent = Niveau ${objectNiv}; const collectionDiv = infoPanel.querySelector(".collection"); collection.forEach(src => { const img = document.createElement("img"); img.src = src; collectionDiv.appendChild(img); }); infoPanel.showModal(); infoPanel.addEventListener("click", () => infoPanel.close()); infoPanel.onclose = () => infoPanel.remove(); } function getEnergyBoost() { const select = document.getElementById("powerEnergy"); if (!select) return 1; const val = select.value; if (val === "2x") return 2; if (val === "3x") return 3; return 1; } /* =============================== DONNÉES JOUEUR ================================ */ const player = { money: 0, energy: MAX_ENERGY, level: 1.0, lastEnergyTick: Date.now() }; /* =============================== OBJETS (NOMS) ================================ */ const OBJECT_NAMES = { CPP1: "pripri simple", CPP2: "pripri double", CPP3: "pripri triple", CPP4: "pripri quadruple", CPP5: "pripri RJ45", CPP6: "pripri USB", CPP7: "pripri 7 USB", CPP8: "⚡ maman pripri", CPP9: "⚡ pripri extraterrestre", RDP1: "boîte vide", RDP2: "⚡ boîte à pripri", RDP3: "⚡ boîte un peu pleine", RDP4: "⚡ boîte bien pleine", RDP5: "⚡ boîte pleine", RDP6: "⚡ ville de priprix", PCS1: "🪙 1 pièce", PCS2: "🪙 3 pièces", PCS3: "🪙 7 pièces", PCS4: "🪙 15 pièces", PCS5: "🪙 32 pièces", ENR1: "⚡ 2 énergies", ENR2: "⚡ 5 énergies", ENR3: "⚡ 15 énergies", ENR4: "⚡ 40 énergies", ENR5: "⚡ 100 énergies", DFU1: "💎 1 fonctionnalité payantes", DFU2: "💎 3 fonctionnalités payantes", DFU3: "💎 7 fonctionnalités payantes", DFU4: "💎 15 fonctionnalités payantes", DFU5: "💎 32 fonctionnalités payantes", CFR1: "sachet bleu", CFR2: "sachet violet", CFR3: "boite transparente", CFR4: "petit coffret", CFR5: "grand coffret", CFR6: "grand coffre", CFR7: "coffre XXL", BBP1: "bébé pripri", BBP2: "pripri ados", BEP1: "bébé pripri extraterrestre", BEP2: "pripri extraterrestre ados", BEP3: "jeune pripri extraterrestre", BEP4: "pripri extraterrestre adulte", BEP5: "vieux pripri extraterrestre", }; /* =============================== MATCH OBJECT ================================ */ class MatchObject { constructor(id, level = 1) { this.id = id; this.level = level; this.locked = false; this.cooldownEnd = 0; } } MatchObject.prototype.productionCount = 0; MatchObject.prototype.cooldownUntil = 0; /* =============================== GRILLE ================================ */ const gridCells = Array.from(document.querySelectorAll("#grid th")); function clearGrid() { gridCells.forEach(c => { c.innerHTML = ""; c.removeAttribute("completed"); c.matchObject = null; }); } function placeObject(cell, obj, isMerge, locked, boxed) { cell.innerHTML = ""; const img = document.createElement("img"); img.src = icons/${obj.id}.png; img.draggable = true; img.dataset.id = obj.id; img.dataset.level = obj.level; if (isMerge) { grid.querySelectorAll("img").forEach((secCurElement) => { secCurElement.removeAttribute("selected"); }); img.setAttribute("selected", ""); const cell = img.parentElement; currentSelectedObject = cell.matchObject; } cell.appendChild(img); cell.setAttribute("completed", ""); if (boxed != undefined || boxed != null) { // si boxed vaut 0, il seras mis quand même (pas de problèmes) cell.setAttribute("boxed", boxed) } if (locked) { cell.setAttribute("locked", "") } cell.matchObject = obj; } /* =============================== INITIALISATION PLATEAU ================================ */ clearGrid(); // objets de départ (exemple jouable) placeObject(gridCells[17], new MatchObject("CPP1", 1), false, true); placeObject(gridCells[23], new MatchObject("RDP1", 1)); placeObject(gridCells[24], new MatchObject("RDP1", 1)); /* =============================== FUSION ================================ */ function canMerge(a, b) { if (!a || !b) return false; if (a.dataset.id !== b.dataset.id) return false; for (const p of NON_FUSIONABLE_PREFIX) { if (a.dataset.id.startsWith(p)) return false; } return Number(a.dataset.level) === Number(b.dataset.level); } function mergeCells(fromCell, toCell) { const a = fromCell.querySelector("img"); const b = toCell.querySelector("img"); if (!canMerge(a, b)) return; const newLevel = Number(a.dataset.level) + 1; const id = a.dataset.id.substring(0, 3) + newLevel; fromCell.innerHTML = ""; fromCell.removeAttribute("completed"); fromCell.matchObject = null; placeObject(toCell, new MatchObject(id, ${newLevel}, true)); } /* =============================== DRAG & DROP ================================ */ let draggedCell = null; gridCells.forEach(cell => { cell.addEventListener("dragstart", e => { if (!cell.matchObject) return; if (e.target.getAttribute("locked")) return; draggedCell = cell; e.target.style.opacity = 0.3; }); cell.addEventListener("dragend", e => { e.target.style.opacity = ""; }); cell.addEventListener("dragover", e => { e.preventDefault(); }); cell.addEventListener("drop", e => { e.preventDefault(); if (!draggedCell || draggedCell === cell) return; if (cell.matchObject) { mergeCells(draggedCell, cell); } else { cell.appendChild(draggedCell.firstChild); cell.matchObject = draggedCell.matchObject; draggedCell.matchObject = null; draggedCell.innerHTML = ""; draggedCell.removeAttribute("completed"); cell.setAttribute("completed", ""); } }); }); /* =============================== DOUBLE CLIC = RÉCUPÉRER ================================ */ gridCells.forEach(cell => { cell.addEventListener("dblclick", () => { if (!cell.matchObject) return; const id = cell.matchObject.id; // pièces if (id.startsWith("PCS")) { const values = [1, 3, 7, 15, 32]; player.money += values[cell.matchObject.level - 1] || 1; } // énergie if (id.startsWith("ENR")) { const values = [2, 5, 15, 40, 100]; player.energy = Math.min( MAX_ENERGY, player.energy + (values[cell.matchObject.level - 1] || 1) ); } if (id.startsWith("PCS") || id.startsWith("ENR")) { cell.innerHTML = ""; cell.matchObject = null; cell.removeAttribute("completed"); } }); }); /* =============================== PRODUCTION AVANCÉE ================================ */ gridCells.forEach(cell => { cell.addEventListener("click", () => { if (!cell.matchObject) return; const obj = cell.matchObject; const id = obj.id; if (!OBJECT_NAMES[id]?.includes("⚡")) return; // cooldown if (obj.cooldownUntil && Date.now() < obj.cooldownUntil) { showMessage("chargement...<br>veillez patienter ou<br>dépencer de l'argent"); return; } if (player.energy <= 0) { openEnergyAssistant(); return; } const emptyCell = gridCells.find(c => !c.matchObject); if (!emptyCell) { showMessage("Plateau plein !"); return; } // énergie consommée const boost = getEnergyBoost(); // vérification énergie if (player.energy < boost) { showMessage("⚠️ Pas assez d'énergie pour ce boost", openEnergyAssistant); return; } player.energy -= boost; document.querySelector(".energy").textContent = player.energy; obj.productionCount++; let producedId = null; let producedLevel = 1; /* ===== RÈGLES ===== */ // PRODUCTEUR PRINCIPAL if (id.startsWith("RDP")) { producedId = "CPP1"; producedLevel = Math.min(7, 1 + (boost - 1)); if (obj.productionCount >= 50) { obj.cooldownUntil = Date.now() + 300000; // 5 min obj.productionCount = 0; } } // MAMAN PRIPRI if (id === "CPP8") { producedId = "BBP1"; producedLevel = Math.min(2, (Math.random() < 0.5 ? 1 : 2) + (boost - 1)); if (obj.productionCount >= 25) { cell.innerHTML = ""; cell.matchObject = null; cell.removeAttribute("completed"); showMessage("💥 La maman pripri s'est désintégrée"); return; } } // PRIPRI EXTRATERRESTRE if (id === "CPP9") { if (Math.random() < 0.6) { producedId = "BBP1"; producedLevel = Math.random() < 0.5 ? 1 : 2; } else { producedId = "BEP1"; producedLevel = Math.min(5, (Math.floor(Math.random() * 5) + 1) + (boost - 1)); } if (obj.productionCount >= 25) { cell.innerHTML = ""; cell.matchObject = null; cell.removeAttribute("completed"); showMessage("👽 Le pripri extraterrestre s'est dissous"); return; } } if (!producedId) return; placeObject( emptyCell, new MatchObject(producedId, producedLevel) ); }); }); /* =============================== ÉNERGIE AUTO ================================ */ setInterval(() => { if (player.energy >= MAX_ENERGY) return; if (Date.now() - player.lastEnergyTick >= ENERGY_REGEN_TIME) { player.energy++; player.lastEnergyTick = Date.now(); } document.querySelector(".energy").textContent = player.energy; }, 1000); /* =============================== UI ================================ */ document.querySelector(".money").textContent = player.money; document.querySelector(".energy").textContent = player.energy; /* =============================== SELECTION ================================ */ setInterval(() => { grid.querySelectorAll("img").forEach((curElement) => { curElement.onclick = () => { grid.querySelectorAll("img").forEach(img => { img.removeAttribute("selected"); }); curElement.setAttribute("selected", ""); const cell = curElement.parentElement; currentSelectedObject = cell.matchObject; }; }); if (grid.querySelector('img[selected]')) { var selection = grid.querySelector('img[selected]').dataset, texteOfBubule = '<strong>' + OBJECT_NAMES[selection.id] + '</strong>' + ' niv. ' + selection.level; if (texteOfBubule.includes('⚡')) { texteOfBubule = '<strong>' + OBJECT_NAMES[selection.id] + '</strong> niv. ' + selection.level + ". PRDUCTEUR consommant de l'ÉNERGIE."; } document.querySelector('.textBuBule text').innerHTML = texteOfBubule; document.querySelector('.textBuBule').removeAttribute('no-selection'); }else{ document.querySelector('.textBuBule text').innerHTML = 'aucun objet selectionné'; document.querySelector('.textBuBule').setAttribute('no-selection', ''); } }, 10); /* ============================== select d'options =============================== */ options.value = 'options'; options.onchange = () => { switch (options.value) { case "informations et propriétées": if (!currentSelectedObject) { showMessage("Aucun objet sélectionné"); break; } const obj = currentSelectedObject; const collection = getCollectionFor(obj.id); moreInfo( obj.level, collection, obj.level - 1 ); break; } options.value = 'options'; } /* =============================== COMMANDES ================================ */ function onCommandCompleted(command) { DAILY_CHALLENGE.tasks.forEach(task => { if ( task.id === "plancequot_orders" && command.person.id === "plancequot" ) { task.progress++; if (task.progress >= task.target) { task.completed = true; } } }); checkDailyChallengeCompletion(); renderDailyChallenge(); } function generateCommand(playerLevel) { const person = COMMAND_PERSONS[Math.floor(Math.random() * COMMAND_PERSONS.length)]; const baseLevel = Math.max(1, Math.floor(playerLevel)); const difficulty = person.levelBonus || 0; const objectLevel = baseLevel + (Math.random() < difficulty ? 1 : 0); const objectId = "CPP" + Math.min(9, objectLevel); return { person, target: { id: objectId, level: objectLevel }, reward: person.noReward ? null : { money: Math.floor(20 + objectLevel * 10), energy: Math.floor(5 + objectLevel * 2), level: person.levelBonus } }; } function initCommands() { const count = 3 + Math.floor(Math.random() * 4); // 3 → 6 activeCommands = []; for (let i = 0; i < count; i++) { activeCommands.push(generateCommand(player.level)); } renderCommands(); } function onCommandCompleted(command) { DAILY_CHALLENGE.tasks.forEach(task => { if ( task.id === "plancequot_orders" && command.person.id === "plancequot" ) { task.progress++; if (task.progress >= task.target) { task.completed = true; } } }); checkDailyChallengeCompletion(); renderDailyChallenge(); } initCommands(); function tryCompleteCommand(matchObject, cell) { const index = activeCommands.findIndex(cmd => cmd.target.id === matchObject.id && cmd.target.level === matchObject.level ); if (index === -1) return false; const cmd = activeCommands[index]; if (cmd.reward) { player.money += cmd.reward.money; player.energy = Math.min(MAX_ENERGY, player.energy + cmd.reward.energy); player.level += cmd.reward.level; } // supprimer l’objet cell.innerHTML = ""; cell.matchObject = null; cell.removeAttribute("completed"); // remplacer la commande activeCommands.splice(index, 1); activeCommands.push(generateCommand(player.level)); renderCommands(); return true; } function generatePlancequotCommand(playerLevel) { return { id: "CMD_PLANCEQUOT_" + Date.now(), person: PERSONS.plancequot, request: { id: "BBP", minLevel: Math.max(2, Math.floor(playerLevel)), quantity: 3 + Math.floor(playerLevel) }, rewards: null // volontairement VIDE }; } if (tryCompleteCommand(cell.matchObject, cell)) { onCommandCompleted(cmd); showMessage("✅ Commande complétée !"); } /* =============================== taches quotidiennes ================================ */ function createPlancequotTask(count) { return { id: "plancequot_orders", description: "Réalise une commande de Plancequot", target: count, progress: 0, completed: false }; } function renderDailyChallenge() { const container = document.querySelector(".daily-challenge .tasks"); container.innerHTML = ""; DAILY_CHALLENGE.tasks.forEach(task => { const div = document.createElement("div"); div.className = "task"; div.innerHTML = <div>${task.description}</div> <div>${task.progress} / ${task.target}</div> ; if (task.completed) div.classList.add("done"); container.appendChild(div); }); } function checkDailyChallengeCompletion() { if (DAILY_CHALLENGE.tasks.every(t => t.completed)) { DAILY_CHALLENGE.completed = true; giveDailyRewards(); } } function giveDailyRewards() { showMessage("🎉 Défi quotidien complété !"); player.energy = Math.min(MAX_ENERGY, player.energy + 30); player.money += 100; // bonus possible // drop coffre rare // boost temporaire } function onPlancequotHelped() { DAILY_CHALLENGE.tasks.forEach(task => { if (task.id === "plancequot_orders") { task.progress++; if (task.progress >= task.target) { task.completed = true; } } }); renderDailyChallenge(); checkDailyChallengeCompletion(); } function tryCompleteCommand(command) { if (!hasRequiredObjects(command.request)) return false; consumeRequestedObjects(command.request); // CAS PLANCEQUOT if (command.person.id === "plancequot") { showMessage("Plancequot vous remercie silencieusement."); onPlancequotHelped(); return true; } // autres personnages (normal) giveRewards(command.rewards); increasePlayerLevel(command.person.levelGain); return true; } document.querySelectorAll(".await-command").forEach(cmdEl => { cmdEl.onclick = () => { const commandId = cmdEl.dataset.commandId; const command = COMMANDS.find(c => c.id === commandId); tryCompleteCommand(command); }; }); /* =============================== FIN SCRIPT ================================ */
